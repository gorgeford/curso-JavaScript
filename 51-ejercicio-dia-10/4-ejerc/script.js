//Reemplazar todos los puntos
//Se supone que el código provisto reemplaza todos los puntos .en la Cadena especificada strcon guiones-

//Pero no está funcionando correctamente.

//Tarea
//Solucione el error para que todos podamos irnos a casa temprano.

//notas
//La cadena strnunca será nula.

var replaceDots = function(str) {
    return str.replace(/[.]/g, '-');
  }
 

console.log(replaceDots(h.k.d.d.d));




  //otras soluciones

  /*
  var replaceDots = function(str) {
  return str.split(".").join("-")
} */
